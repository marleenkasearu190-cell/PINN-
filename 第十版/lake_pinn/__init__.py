"""Tenth-version zero-profile reconstruction-state LakePINN package.

The active public API is exposed through ``lake_pinn.api``.  The default CLIs
are ``python -m lake_pinn`` for single-lake state reconstruction and
``python -m lake_pinn.state_multilake`` for multi-lake reconstruction-state training.
Version 10 is the development source for zero-profile RECON diagnostics; legacy
few-shot artifacts and older version checkpoints are retained only as historical
references unless an experiment explicitly selects them.
"""

__all__ = []
