# 第一版

这是当前仓库默认使用的代码版本。

如果你准备继续开发、修复问题、补充文档或运行主流程，请优先从这个目录开始，而不是 `第零版`。

## 目录结构

- `下载爬取/`：负责 ERA5、MODIS LST 等数据下载与转换
- `数据处理/`：负责可视化、预测建模和结果验证

## 推荐阅读顺序

1. 先看 `下载爬取/`
2. 再看 `数据处理/`
3. 需要回看旧实现时，再参考仓库根目录下的 `第零版/`

## 说明

- 本目录是当前默认入口
- 新改动原则上优先放在本目录
- `第零版/` 仅作为历史留档保留
